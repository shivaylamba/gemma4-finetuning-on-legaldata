# ============================================================================
# Pinned dependencies for Gemma legal fine-tuning (CUDA 12.4 / H100).
#
# Install with:
#   pip install --index-url https://download.pytorch.org/whl/cu124 \
#       torch==2.6.0 torchvision==0.21.0
#   pip install -r requirements.txt
#
# For the serving stack (api.py / vLLM) also install:
#   pip install -r requirements-serve.txt
# ============================================================================

# --- Hugging Face stack (training) ------------------------------------------
transformers==5.5.0
accelerate==1.6.0
peft==0.14.0
trl==0.15.2
datasets==3.5.1
bitsandbytes==0.45.5
safetensors==0.5.3
huggingface_hub[cli]==0.30.2

# --- Tokeniser / model extras -----------------------------------------------
sentencepiece==0.2.1
protobuf==5.29.6
einops==0.8.1

# --- Optional: Flash Attention 2 (H100 speedup) -----------------------------
# Uncomment the line below or run:
#   pip install --no-build-isolation "flash-attn==2.7.4"
# flash-attn==2.7.4
